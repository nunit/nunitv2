<?xml version="1.0"?>
<project name="NUnit.Console.Tests" default="build">

    <!--
        Required properties:
            * bin.dir               - bin directory
            * src.dir               - source location(build) or target(copy)
            * build.debug           - (true|false) debug build?
            * build.mono            - (true|false) mono build?
            * build.defines.csc     - build defines for build config
    -->

    <target name="build" depends="copy">

        <csc target="library" output="${bin.dir}/nunit-console.tests.dll" debug="${build.debug}" define="${build.defines.csc}">
            <sources basedir="${src.dir}/ConsoleRunner/tests">
                <include name="*.cs"/>
            </sources>
            <references basedir="${bin.dir}">
                <include name="nunit.framework.dll"/>
                <include name="nunit.framework.tests.dll"/>
                <include name="nunit-console-runner.dll"/>
            </references>
        </csc>

    </target>

    <target name="copy">

        <copy todir="${src.dir}/ConsoleRunner/tests">
            <fileset basedir=".">
                <include name="nunit.util.tests.csproj"/>
                <include name="nunit.util.build"/>
                <include name="AssemblyInfo.cs"/>
                <include name="ConsoleRunnerTest.cs"/>
            </fileset>
        </copy>

    </target>
</project>
