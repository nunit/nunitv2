<?xml version="1.0"?>
<project name="NUnitCore" default="build">

  <property name="project.src.dir" value=".."
    unless="${property::exists('project.src.dir')}"/>

  <include buildfile="../nunit.build.include"/>
  
  <target name="clean">
    <nant buildfile="common/nunit.common.build" 
      target="clean"/>
    <nant buildfile="core/nunit.core.build" 
      target="clean"/>
    <nant buildfile="tests/nunit.core.tests.build"
      target="clean"/>
  </target>

  <target name="build">
    <nant buildfile="common/nunit.common.build" 
      target="build"/>
    <nant buildfile="core/nunit.core.build" 
      target="build"/>
  </target>

  <target name="build-tests">
    <nant target="build"
      buildfile="tests/nunit.core.tests.build"/>
  </target>

  <target name="package">
    <nant buildfile="common/nunit.common.build" 
      target="package"/>
    <nant buildfile="core/nunit.core.build" 
      target="package"/>
    <nant buildfile="tests/nunit.core.tests.build"
      target="package"/>

    <copy todir="${package.src.dir}/NUnitCore"
      file="NUnitCore.build"/>
  </target>

</project>